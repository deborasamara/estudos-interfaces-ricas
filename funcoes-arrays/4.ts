/**
 * 
 4. Faça um programa que leia o array pegue apenas os dois primeiros elementos. Use o método SLICE da classe Array.
 */

 export function primeiros_elementos(array:number[]): number[]{
    return array.slice(0,2);
 }
