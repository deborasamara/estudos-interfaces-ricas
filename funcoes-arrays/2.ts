/**
2. Faça um Programa TypeScript que transforme o array, concatenando as String com 1 (um) espaço (“ “). Utilize o método JOIN da classe Array.
 */

export function concatenaArray(array:string[]): string{
    return array.join(" ");
}

